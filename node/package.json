{
  "name": "@tana/node",
  "version": "0.1.0",
  "type": "module",
  "description": "Tana blockchain node - P2P networking, consensus, and block validation",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test"
  },
  "dependencies": {
    "@libp2p/tcp": "^10.0.0",
    "@libp2p/websockets": "^9.0.0",
    "libp2p": "^1.0.0",
    "postgres": "^3.4.0",
    "drizzle-orm": "^0.30.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.20.0"
  }
}
